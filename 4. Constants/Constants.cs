namespace _4._Constants
{
    internal class Constants
    {
        static void Main(string[] args)
        {
            const int MAX_SCORE = 100;

            Console.WriteLine("\n\tThe maximum score is: " + MAX_SCORE);

            /*
            
            int MAX_SCORE = 100;
            Console.WriteLine("\n\tThe maximum score is: " + MAX_SCORE);

            MAX_SCORE = 200;
            Console.WriteLine("\n\tThe score has increased to: " + MAX_SCORE);
            
            */
        }
    }
}